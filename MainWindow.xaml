<Window x:Class="TailorShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة عملاء محل الخياطة" Height="600" Width="900" FlowDirection="RightToLeft" Background="#E0F7FA">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#87CEEB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#F0F8FF"/>
            <Setter Property="BorderBrush" Value="#87CEEB"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <TextBlock x:Name="Watermark" Text="{TemplateBinding Tag}" Foreground="Gray" FontStyle="Italic" Margin="5" IsHitTestVisible="False" Opacity="0.7"
                                       VerticalAlignment="Center" HorizontalAlignment="Right" Visibility="Hidden"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsKeyboardFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#F0F8FF"/>
            <Setter Property="BorderBrush" Value="#87CEEB"/>
            <Setter Property="AlternatingRowBackground" Value="#E6F0FA"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="dgCustomers" Grid.Column="0" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn  Header="م" Binding="{Binding ID}" IsReadOnly="True"/>
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="الطول" Binding="{Binding Length}"/>
                <DataGridTextColumn Header="الكم" Binding="{Binding Sleeve}"/>
                <DataGridTextColumn Header="الكتف" Binding="{Binding Shoulder}"/>
                <DataGridTextColumn Header="الوسع" Binding="{Binding Width}"/>
                <DataGridTextColumn Header="اللياقة" Binding="{Binding Fitness}"/>
                <DataGridTextColumn Header="الطوق" Binding="{Binding Collar}"/>
                <DataGridTextColumn Header="التطريز" Binding="{Binding Embroidery}"/>
                <DataGridTextColumn Header="التليفون" Binding="{Binding Phone}"/>
                <DataGridTextColumn Header="ملاحظات" Binding="{Binding Notes}"/>
                <DataGridTemplateColumn Header="الإجراءات" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="حذف" Click="BtnDelete_Click" Tag="{Binding ID}" Width="50"/>
                                <Button Content="تعديل" Click="BtnUpdate_Click" Tag="{Binding ID}" Width="50" Margin="5,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1" Margin="10">
            <Label Content="الاسم:"/>
            <TextBox x:Name="txtName" Width="200" Tag="الاسم"/>
            <Label Content="الطول:"/>
            <TextBox x:Name="txtLength" Width="200" Tag="الطول"/>
            <Label Content="الكم:"/>
            <TextBox x:Name="txtSleeve" Width="200" Tag="الكم"/>
            <Label Content="الكتف:"/>
            <TextBox x:Name="txtShoulder" Width="200" Tag="الكتف"/>
            <Label Content="الوسع:"/>
            <TextBox x:Name="txtWidth" Width="200" Tag="الوسع"/>
            <Label Content="اللياقة:"/>
            <TextBox x:Name="txtFitness" Width="200" Tag="اللياقة"/>
            <Label Content="الطوق:"/>
            <TextBox x:Name="txtCollar" Width="200" Tag="الطوق"/>
            <Label Content="التطريز:"/>
            <TextBox x:Name="txtEmbroidery" Width="200" Tag="التطريز"/>
            <Label Content="التليفون:"/>
            <TextBox x:Name="txtPhone" Width="200" Tag="التليفون"/>
            <Label Content="ملاحظات:"/>
            <TextBox x:Name="txtNotes" Width="200" Tag="ملاحظات" Height="50" AcceptsReturn="True"/>
            <Button x:Name="btnAdd" Content="إضافة" Click="btnAdd_Click" Width="200" Margin="0,10,0,0"/>
            <TextBox x:Name="txtSearch" Width="200" Tag="بحث بالاسم" Margin="0,10,0,0"/>
            <Button x:Name="btnSearch" Content="بحث" Click="btnSearch_Click" Width="200"/>
        </StackPanel>
    </Grid>
</Window>